= Тестирање

== Приступ тестирању

Квалитет backend кода проверен је искључиво јединичним (unit) тестовима, писаним уз употребу Rust-ovog уграђеног тест framework-a (`#[test]` атрибут, покретање командом `cargo test`), без додатног екстерног оквира за тестирање. Тестови су смештени непосредно уз код који тестирају, у модулу означеном атрибутом `#[cfg(test)]` на дну сваког изворног фајла — конвенција коју Rust заједница преферира, с обзиром да тестови тако имају приступ и приватним (не само јавним) функцијама и структурама модула, а `#[cfg(test)]` обезбеђује да се тест код уопште не компајлира у финални (release) бинарни фајл.

Укупан број јединичних тестова у пројекту износи 62, распоређених по сервисима као у табели 14.


#figure(
  table(
    columns: 3,
    [*Сервис*], [*Број тестова*], [*Фокус тестирања*],
    [Analysis Service], [43], [Детектори (code smell, перформансе, безбедност, дупликација), дедупликација налаза],
    [Parser Service], [12], [Извлачење функција/класа из AST-a (Go, Java, Rust)],
    [ML Ranker Service], [7], [Формула рангирања, калибрација озбиљности, хијерархија категорија],
    [Auth Service], [0], [—],
    [Suggestion Generator Service], [0], [—],
    [API Gateway], [0], [—],
  ),
  caption: [Преглед јединичних тестова по сервису]
) <tab-14>

Тестирање је, у тренутној фази пројекта, намерно усмерено на сервисе са највише самосталне, детерминистички проверљиве пословне логике (детекција проблема, парсирање, рангирање) — сервиси који су претежно танки слојеви над HTTP-om, базом података или спољним сервисима (Auth, Suggestion Generator, API Gateway) тренутно немају сопствене јединичне тестове, што је наведено као ограничење у поглављу 6.5, а не као превид који би требало да остане неописан.

== Шаблон тестних података (test fixtures)

Да би се избегло понављање исте дугачке иницијализације у сваком тесту, тестови у Analysis сервису доследно користе једноставан образац "подразумевано па изузетак" (Листинг 15): помоћна функција враћа валидну, "чисту" инстанцу улазних података (функцију без иједног проблема), а сваки тест мења само оно поље које је релевантно за случај који проверава, пре него што га проследи детектору.

#figure(
```rust
fn base_function() -> FunctionInfo {
    FunctionInfo {
        name: "test_fn".into(),
        lines_of_code: 10,
        parameter_count: 2,
        nesting_depth: 1,
        cyclomatic_complexity: 3,
        // ... ostala polja sa "bezbednim" podrazumevanim vrednostima
    }
}

fn make_function(f: FunctionInfo) -> ParsedAst { /* umotava FunctionInfo u ParsedAst */ }
```,
  caption: [Образац тестних података "подразумевано па изузетак" (analysis_service/src/detectors/smells.rs)]
) <lst-15>

Захваљујући овом обрасцу, тест попут оног у поглављу 6.3 своди се на једну измењену вредност поља (`f.lines_of_code = 51`), чиме је намера теста читљива на први поглед, без потребе да читалац размрси које су вредности случајне, а које битне за проверу.

== Гранични (boundary) тестови

За правила заснована на нумеричким праговима (табела 7), тестови доследно проверавају понашање тачно на граници и непосредно преко ње, а не само "очигледне" случајеве дубоко унутар или ван опсега. Пример за правило "дуга метода" (праг 50 линија):

#figure(
```rust
#[test]
fn long_method_fires_above_50_loc() {
    let mut f = base_function();
    f.lines_of_code = 51;
    let problems = SmellDetector::new().detect(&make_function(f));
    assert!(problems.iter().any(|p| p.problem_type == "code_smell.long_method"));
}

#[test]
fn long_method_does_not_fire_at_50_loc() {
    let mut f = base_function();
    f.lines_of_code = 50;
    let problems = SmellDetector::new().detect(&make_function(f));
    assert!(!problems.iter().any(|p| p.problem_type == "code_smell.long_method"));
}
```,
  caption: [Гранични тест услова "> 50 линија" (analysis_service/src/detectors/smells.rs)]
) <lst-16>

Пар тестова попут овог експлицитно потврђује да је услов у коду заиста строго веће (`> 50`), а не веће-или-једнако — разлика која се лако погрешно напише, а чију би последицу (правило које се окида један ред раније или касније него што је намеравано) обичан "срећни пут" тест не би открио. Исти образац се доследно понавља за све остале прагове у табели 7 (параметри, угњеждавање, комплексност, величина класе/фајла), као и за праг озбиљности (нпр. `long_method_severity_high_above_100_loc`, који проверава да озбиљност прескочи на `High` тачно преко другог прага).

== Регресиони тестови

Више тестова у Analysis сервису је експлицитно везано за конкретан, раније уочен и исправљен пропуст у детекцији (поглавље 5.3.5, 5.3.6), а не за опште понашање правила. Тест `detects_aws_secret_key_regression` (Листинг 17) је најдиректнији пример:

#figure(
```rust
#[test]
fn detects_aws_secret_key_regression() {
    let code = "aws_secret_key = \"wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY\"\n";
    let problems = SecurityDetector::new().detect(&ast_with_code(code, Language::Python));
    assert!(
        problems.iter().any(|p| p.problem_type == "security.hardcoded_secret"),
        "aws_secret_key mora biti detektovan kao hardkodovan secret"
    );
}
```,
  caption: [Регресиони тест за исправљен образац препознавања AWS креденцијала (analysis_service/src/detectors/security.rs)]
) <lst-17>

Овакви тестови имају другачију сврху од граничних тестова из поглавља 6.3 — не проверавају границу правила, већ конкретан улаз који је у ранијој верзији regexa пролазио недетектован, и тиме трајно штите од поновног увођења исте грешке при будућим изменама обрасца. Исти приступ је примењен и за детекцију Н+1 упита (`no_false_positive_when_select_is_outside_any_loop`, поглавље 5.3.5), где тест проверава да SQL упит дефинисан изван било које петље више не генерише лажан налаз, што је био недостатак раније, поједностављене имплементације.

== Тестирање модела рангирања

Како ML Ranker Service (поглавље 5.4) није тренирани модел, уобичајене мере попут прецизности (accuracy) или Ф1 мере нису примењиве — уместо тога, тестови проверавају да формула рангирања задовољава скуп пословно смислених инваријанти, односно да се рангирање понаша у складу са намером дизајна, без обзира на тачне нумеричке вредности коефицијената. Листинг 18 приказује тест који проверава да хијерархија тежина категорија (табела 9) остаје строго опадајућа.

#figure(
```rust
#[test]
fn category_hierarchy() {
    let engine = MLEngine::new();
    assert!(engine.category_weight("security.hardcoded_secret") > engine.category_weight("security.sql_injection"));
    assert!(engine.category_weight("security.sql_injection")     > engine.category_weight("security.insecure_random"));
    assert!(engine.category_weight("security.insecure_random")   > engine.category_weight("performance.n_plus_one"));
    assert!(engine.category_weight("performance.n_plus_one")     > engine.category_weight("code_smell.duplicate_code"));
    assert!(engine.category_weight("code_smell.duplicate_code")  > engine.category_weight("code_smell.magic_number"));
}
```,
  caption: [Тест монотоности хијерархије категорија (ml_ranker_service/src/ml_engine.rs)]
) <lst-18>

Слично, тест `severity_calibration_bands` проверава да функција калибрације (поглавље 5.4.6) сваки од четири нивоа озбиљности мапира у тачно одређен, међусобно раздвојен опсег (Critical ≥ 0.80; High у [0.58, 0.80); Medium у [0.30, 0.58); Low у [0.08, 0.30)) и да је поредак строго опадајући (`c > h > m > l`), док тестови `hardcoded_secret_scores_highest`, `security_outranks_style` и `main_file_higher_than_test_file` проверавају да крајња формула рангирања (поглавље 5.4.5), укључујући и мултипликативни фактор изложености фајла, даје очекиван релативни поредак на реалистичним примерима проблема — нпр. да исти тип проблема у `main.py` добија већу оцену него идентичан проблем у `test_main.py`, а тест `scores_in_unit_range` проверава да крајња оцена, независно од комбинације улазних обележја, никада не изађе из опсега [0, 1].

== Ограничења приступа тестирању

Ради транспарентности, потребно је навести границе тренутног приступа тестирању:

- *Нема јединичних тестова за Auth, Suggestion Generator и API Gateway сервисе.* Ови сервиси су, за разлику од Analysis/Parser/ML Ranker, претежно организовани око I/O операција (HTTP позиви, упити ка бази, RabbitMQ) које захтевају mock или тест-контејнер инфраструктуру да би се тестирале изоловано; та инфраструктура није успостављена у тренутној фази пројекта. Шаблонски механизам Suggestion Generator сервиса (поглавље 5.5.3), иако чисто детерминистички и лако тестабилан без спољних зависности, такође тренутно нема покривеност тестовима.
- *Нема интеграционих тестова.* Сви постојећи тестови проверавају појединачне функције/модуле у изолацији (нпр. један детектор над ручно конструисаним `ParsedAst`), али ниједан тест тренутно не покреће више сервиса заједно нити проверава стварну размену порука преко RabbitMQ-a, уписивање у праве инстанце база података, или цео ток података описан у поглављу 4.5 од краја до краја (end-to-end).
- *Нема аутоматизованог CI pipeline-a* који би тестове покретао при свакој измени кода — тестови се у тренутној фази покрећу ручно, командом `cargo test`, по појединачном сервису или над целим workspace-om.

Ови недостаци су свесно остављени ван оквира дипломског рада и наведени су као правци даљег развоја у поглављу 8, уместо да буду приказани као већ решени.
